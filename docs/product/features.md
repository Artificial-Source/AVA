# AVA Feature Matrix

This is a user-facing summary of AVA's current product shape. [Product principles](principles.md) and the [release-readiness ledger](release-readiness.md), not competitor parity, govern scope. The Pi.dev column preserves comparative context from the dated audit; it is not current execution evidence.

Status terms:

- **Done**: implemented for AVA's current MVP target and backed by tests, smokes, or documented evidence.
- **Partial**: useful behavior exists, but AVA is intentionally narrower than Pi or still lacks breadth/evidence.
- **Deferred**: valid future work, but outside the current MVP until a design or approval gate exists.
- **Intentional divergence**: AVA chooses a different product/safety shape rather than copying Pi.

| Area | Pi baseline | AVA status | AVA behavior and gap |
| --- | --- | --- | --- |
| Providers | Broad provider registry, generated catalogs, env auth, OAuth, compatibility settings, and image-generation entries. | **Partial** | OpenAI, Anthropic, DeepSeek, Gemini, Kimi, Moonshot, OpenRouter-compatible, Z.AI, xAI, Groq, Cerebras, Together, Fireworks, and Mistral chat/responses paths are implemented, plus user-defined providers via `providers.json`. Vertex, Bedrock, Azure/Copilot, and other remaining provider-zoo routes stay deferred until provider-specific auth, metadata, quirks, and live-smoke plans exist. The six declarative generic built-ins have offline contract tests only (no live qualification). |
| Provider auth | API keys and OAuth across selected providers. | **Partial** | OpenAI browser/device OAuth and API keys are implemented; provider-scoped API keys are supported; Anthropic stored/env OAuth bearer refresh is supported. Anthropic interactive OAuth and cloud-specific auth chains are deferred where no supported third-party flow or product design exists. |
| Model catalog | Generated model/image catalogs and broad provider metadata. | **Intentional divergence** | AVA uses a curated built-in catalog plus `models.json` overrides so release behavior is reviewable and not changed by build-time/runtime network fetches. Generated catalog sync and OpenRouter image generation are deferred. |
| Reasoning/thinking | Provider-specific thinking and reasoning controls. | **Done** | AVA validates reasoning controls against model metadata, stores/replays provider-native reasoning where safe, emits reasoning events, and lets the TUI hide/show visible thinking without changing provider mode. Completed long thinking uses a bounded 12-row preview with per-item expand/collapse (`Thinking:` click or `/thinking details`); live streaming reasoning stays full. |
| Cross-provider reasoning replay | Reasoning history can exist across model/provider changes. | **Done** | Switching is immediate. Each request builds a copy-only target-aware portable projection: exact-compatible native history can replay natively; incompatible private/native reasoning and metadata are stripped; visible text and complete representable tool pairs continue. Persisted session history is unchanged, and provider-native cross-provider reasoning replay is not claimed. The next request still fails closed for malformed committed v4 output or a committed v4 function call without its exact bound result; projection does not repair invalid history. |
| Sessions | Append-only JSONL sessions with resume, tree/fork/clone, compaction, export, and session mobility. | **Done** | AVA has append-only JSONL sessions, resume/list/stats, compaction, usage/cost records, names/labels/archive, tree/fork/clone, `--session-id`, `--resume`, `--fork`, Markdown/HTML/AVA-JSONL export, and local AVA-JSONL import. |
| Session sharing/import portability | Import/export/share workflows, including public sharing. | **Partial** | Local export/import and the explicit, user-confirmed abandoned-parent provider branch-summary flow are implemented. Pi JSONL conversion, public/cloud sharing, dedicated RPC import/share, and broad automatic branch summarization remain deferred. Use `/export html <path>` for local sharing. |
| TUI/editor/terminal | Polished terminal coding UI with editor, autocomplete, markdown, selectors, images, and terminal protocol handling. | **Done** | AVA has a native `ncursesw` TUI with multiline editing, bounded prompt stash, history/user-turn navigation, double/triple-click transcript selection, transient scrollbar, bounded OSC52 latest-assistant copy, slash/file/path completion, model/session/settings selectors, permissions/tool cards, Markdown/code/diff rendering, disabled-by-default literal Mermaid projection, cursor/Alacritty handling, themes/keybindings, image fallback, and PTY/tmux evidence. Interactive AVA requires a terminal on both stdin and stdout; use print or RPC for non-TTY runs. Keyboard TUI coverage is not screen-reader certification. |
| TUI architecture and tests | Reusable TypeScript TUI components and virtual-terminal tests. | **Intentional divergence** | AVA keeps a native C++ terminal stack and validates with deterministic renderer/editor tests, 23 gated tmux scenarios (including Mermaid projection, nested settings, startup overview, abandoned-parent branch summary, host-rendered plugin UI, and the live read-only subagent workspace), and gated Kitty, iTerm2, terminal-lifecycle, and OSC8/OSC52 PTY tests. A Pi-style virtual terminal or component rewrite is deferred unless current evidence becomes insufficient. |
| Built-in tools | Core read/write/edit/bash/grep/find/ls tool shape. | **Done** | AVA maps Pi aliases to native tools and adds `apply_patch`, `webfetch`, `websearch`, `skill`, `task`, `question`, plugin/MCP tools, and capability-gated LSP tools. Tool output is bounded and side effects route through permission/audit paths. |
| Permissions and trust | Pi relies more on project trust; no built-in operation-level permission popup. | **Intentional divergence** | AVA keeps stronger allow/ask/deny policy, TUI/RPC/headless resolvers, durable rules outside model-writable workspace files, session grants, hard denies, remembered choices, and permission audit/diagnose/export flows. |
| Parallel ordinary tool calls | Pi can batch/parallelize ordinary tool calls. | **Partial** | AVA defaults ordinary model-dispatched tools to sequential execution. A backend-only internal `AgentLoopOptions` opt-in can parallelize preflight-proven builtin read/search calls (`read_file`, `list_directory`, `glob`, `grep`) with provider-order commits, but there is no public CLI/TUI/RPC flag or user-facing/default parallel-tools claim yet. |
| Subagents/background jobs | Pi does not make task subagents a core baseline in the same way. | **Done** | AVA has configurable primary agents selected with invocation-local `--agent`, plus a model-visible `task` tool, built-in and configured subagents, foreground child sessions, and background jobs with `job_id`. Task launch is prompt-free and audited unless an exact persisted deny matches; only sensitive nested or child actions prompt. Foreground nested Ask actions use the normal permission UI, while background nested Ask actions fail closed. Interactive `/jobs` is human text (TUI bare `/jobs` is the live selector; non-TUI bare `/jobs` and `/jobs list` merge live jobs with display-only recorded history; `/jobs history` is recorded history only; show/wait/result/cancel/promote; display ordinals visual only; exact `job_id` authority; only `result` includes bounded terminal content; wait/cancel/promote reject historical-only ids) while public model/RPC JSON remains process-local `schema_version:1`. Cancellation/status tracking and recursive-task hiding remain enforced. Live jobs, results, and pending delivery are process-local for the current AVA process and are not recovered or delivered after close, crash, or restart; parent session JSONL keeps bounded display-only job history and child-session JSONL remains durable and inspectable. |
| Broader multi-agent orchestration | Richer chained agents and task graphs. | **Deferred** | Background `task` is the supported concurrent subagent path. Chained task graphs, plugin-contributed subagent packages, and broader orchestration are later work. |
| Plugins | Pi extensions, skills, prompts, package resources, UI hooks, and custom providers. | **Partial** | AVA has out-of-process local plugins with tools, commands, prompts, skills, events, diagnostics, enablement, a sample plugin, compatibility policy, and bounded attributed host-rendered `status`/`widget`/`select`/`confirm` UI for direct foreground TUI commands. Arbitrary render slots/terminal control, plugin keybindings/themes, custom providers, request interception, marketplace, and remote install remain deferred. |
| MCP | Not a Pi core feature; broader reference systems support advanced MCP transports. | **Partial** | AVA supports a bounded local stdio MCP slice with explicit config, permissioned launch/connect/tool/resource reads, prompt commands, and output bounds. Remote HTTP, OAuth, subscriptions, sampling, templates, binary/blob resources, and daemon pooling are deferred. |
| LSP/code intelligence | Coding ergonomics and code intelligence. | **Partial** | AVA has explicit config plus one global exact-opt-in installed-only `clangd` recipe, pull/publish diagnostics, document/workspace symbols, definitions, references, bounded on-disk full-text sync, logical per-root routing, and identity-bound `lsp.server.launch` permission. This is the sole automatic LSP recipe; every other server requires explicit configuration, and unsaved-buffer sync remains deferred. |
| Platform/install | Local terminal coding agent distribution. | **Partial** | AVA is a native C++23 Linux terminal app. The first-publication target is Linux x64 only and remains blocked on exact retained native evidence. The audited host artifact requires BMI2, GLIBC/GLIBCXX/CXXABI floors, ncursesw/tinfo, and curl documented in the release ledger. No broad platform/package-manager support is claimed. |
| Packages/resource manager | Package install/remove/update/list/config and bundled resource filters. | **Deferred** | `/packages` and `ava packages ...` are recognized but disabled with an explanation. Remote npm/git/marketplace/self-update behavior needs source allowlists, provenance/signing, compatibility, rollback, and trust UX first. Manual resource install paths are documented. |
| Sandboxing | Trust and execution safety around tools/extensions. | **Intentional divergence** | AVA provides permission policy, process containment, output bounds, and audit, but does not claim OS/container sandbox guarantees for tools, plugins, MCP, or shell commands. Real sandboxing is outside the current MVP unless backed by enforcement and tests. |
| Offline/update/telemetry | Offline mode, package/update checks, and analytics choices. | **Done** | AVA does not perform telemetry, self-update, package update checks, remote catalog fetches, or live price/model updates. `--offline` is implemented as a fail-closed provider-call disable for prompt turns and provider-backed compaction; local commands remain available, and network-capable tools still require normal visibility plus permission policy. |

## Main unresolved gaps

- Broader provider breadth and live-provider evidence remain credential/API-specific.
- Pi JSONL conversion, public share, package/resource installs, and self-update remain deferred.
- Public/default ordinary-tool parallelism remains deferred; only the internal read/search backend opt-in exists.
- AVA does not claim OS/container sandboxing.
- Session-tree transcript/content filters, LaTeX, arbitrary extension-owned renderers, blocking startup probes, and broad screen-reader certification are not part of the current TUI contract. Broader physical-terminal/external-workload profiling and richer multi-agent graphs remain future work; deterministic frontend performance budgets are complete.

Source docs: [product baseline](mvp-baseline.md), [coverage ledger](mvp-coverage-ledger.md), [usage](../core/usage.md), [configuration](../core/configuration.md), [testing](../operations/testing.md), [headless protocol](../headless-protocol.md), [plugin system](../extensions/plugin-system.md), and [MCP support](../extensions/mcp.md).
